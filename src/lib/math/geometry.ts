export const circleArea = (r: number) => Math.PI * r * r
export const circleCircumference = (r: number) => 2 * Math.PI * r
export const rectangleArea = (a: number, b: number) => a * b
export const triangleArea = (base: number, height: number) => 0.5 * base * height
